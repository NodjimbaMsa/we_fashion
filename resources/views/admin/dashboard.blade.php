@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Dashboard') }}</div>

                <div class="card-body">
                    @if (session('status'))
                    <div class="alert alert-success" role="alert">
                        {{ session('status') }}
                    </div>
                    @endif

                    <div class="row">
                        <div class="col-sm-6">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">Produits</h5>
                                    <p class="card-text">Cliquer sur le button pour gérer les catégories</p>
                                    <a href="{{route('products.index')}}" class="btn btn-primary">Explorer</a>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">Catégories</h5>
                                    <p class="card-text">Cliquer sur le button pour gérer les catégories</p>
                                    <a href="{{route('products.index')}}" class="btn btn-primary">Explorer</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
